# 无人机 RID 检测 App — 实际测试指引

> 适用版本：v1.0.0（当前 main 分支）
> 测试环境：**必须使用 Android 真机**（高德 3D SDK 仅支持 ARM 架构，x86 模拟器无法加载地图；RID 蓝牙/Wi-Fi 扫描也需要真实射频硬件）。

---

## 0. 测试前准备

### 0.1 安装 APK

方式一（Android Studio）：
1. 打开项目，连接真机（开启「开发者选项 → USB 调试」）
2. 点击 Run ▶ 或直接安装：`adb install app/build/outputs/apk/debug/app-debug.apk`

方式二（命令行构建安装）：
```bash
gradlew.bat assembleDebug
adb install -r app/build/outputs/apk/debug/app-debug.apk
```

### 0.2 高德地图 Key 检查（地图空白时必查）

1. 确认 `C:\Users\<用户名>\.gradle\gradle.properties` 中存在：
   ```
   amapApiKey=f3c171f91d7be2ee98c38708f2a23e75
   ```
2. 登录 [高德开放平台控制台](https://lbs.amap.com)，确认该 Key 绑定的：
   - **发布版安全码 SHA1** = `3C:35:CA:DE:A8:34:11:A4:52:30:EA:66:59:D9:0F:B0:A3:37:CA:36`（debug 签名）
   - **PackageName** = `com.global_707.drone_scanner`
3. 若重新生成过 debug keystore（`~/.android/debug.keystore`），SHA1 会变化，需在高德控制台重新绑定并重建 APK。

### 0.3 单元测试（可选，验证 RID 解析正确性）

```bash
gradlew.bat testDebugUnitTest
```
预期：`BluetoothRidParserTest` 5 个用例全部通过（25 字节位置解析、Basic ID、Message Pack、垃圾载荷、未知值）。

---

## 1. 功能测试用例

### 1.1 首次启动与权限

| # | 步骤 | 预期 |
|---|------|------|
| 1.1.1 | 冷启动 App | 弹出权限请求：位置（必需）、蓝牙（Android 12+）、附近设备（Android 13+） |
| 1.1.2 | 全部允许 | 右上角扫描按钮显示绿色圆点 +「扫描中」 |
| 1.1.3 | 拒绝权限后重新进入 | 按钮显示红色「权限不足」；点击顶部红色提示可重新请求权限 |

### 1.2 首页（地图主页）

| # | 步骤 | 预期 |
|---|------|------|
| 1.2.1 | 观察地图 | 显示真实高德地图（标准样式），可拖动、缩放、旋转 |
| 1.2.2 | 右上角扫描按钮 | 位置固定不闪烁；状态随真实扫描状态变化（扫描中/已停止/扫描异常/权限不足/扫描已关闭） |
| 1.2.3 | 点击扫描按钮 | 扫描状态面板从按钮位置缩放淡入（右上角向下展开），不遮挡地图控件 |
| 1.2.4 | 点击面板外任意位置 | 面板缩放淡出关闭 |
| 1.2.5 | 面板内容 | Wi-Fi / 蓝牙两行显示真实状态：扫描中（转圈）、已关闭、失败原因（如"系统蓝牙未开启"） |
| 1.2.6 | 面板底部按钮 | 「前往搜索设置」跳转到 设置 → 搜索设置 |
| 1.2.7 | 左上角图层按钮 | 切换标准/卫星地图，地图实时切换 |
| 1.2.8 | 左上角定位按钮 | 地图镜头移动到本机真实位置（需已授权定位） |
| 1.2.9 | 左上角北向按钮 | 地图旋转角度归零（正北朝上） |
| 1.2.10 | 底部列表（收起态） | 面板常驻约 1/3 屏，显示「已检测无人机 + 数量」及列表前几项 |
| 1.2.11 | 上拉底部面板 | 展开至约 85% 全屏列表；松手有回弹动画；下拖回收起态 |
| 1.2.12 | 列表项内容 | 呼号（ID 后四位）+ 距离 + 高度 + 状态圆点 + 更新时间；点按有水波纹反馈 |
| 1.2.13 | 空状态 | 无检测结果时显示「未检测到无人机 + 扫描提示」 |

### 1.3 演示模式（无无人机时预览界面）

| # | 步骤 | 预期 |
|---|------|------|
| 1.3.1 | 设置 → 关于我们 → 演示模式开关 | 打开后返回首页 |
| 1.3.2 | 首页列表 | 出现 3 架演示无人机，标题旁有黄色「演示」标签 |
| 1.3.3 | 地图标记 | 地图上出现 3 个彩色标记（颜色各不相同），位于上海坐标（31.23, 121.47）附近 |
| 1.3.4 | 关闭演示模式 | 列表恢复真实检测结果（通常为空） |

### 1.4 飞机详情页

| # | 步骤 | 预期 |
|---|------|------|
| 1.4.1 | 点击列表项进入详情 | 顶部 M3 导航栏（返回箭头 + 呼号） |
| 1.4.2 | 上半屏地图 | 显示三个标记：飞机（彩色）、遥控站（绿色）、我的位置（蓝色），遥控站与飞机间有虚线 |
| 1.4.3 | 全屏查看按钮 | 进入全屏地图页，可返回 |
| 1.4.4 | 「导航到飞机」按钮 | 调起高德/百度/Google 等已安装导航应用，目的地为飞机经纬度 |
| 1.4.5 | 「导航到遥控站」按钮 | 同上，目的地为遥控站经纬度（无遥控站数据时按钮置灰） |
| 1.4.6 | 下半屏键值对表格 | 基本信息/运动状态/操控者三组：序列号、水平/垂直速度、气压高度、地理高度、起飞点高度、航向、RID 时间戳、经纬度、操作员 ID 等；未知字段显示「—」 |
| 1.4.7 | 返回 | 系统返回键/顶部返回箭头均可返回首页 |

### 1.5 设置页面

| # | 步骤 | 预期 |
|---|------|------|
| 1.5.1 | 设置主页 | 仅两组：搜索设置（🔍）/ 显示设置（🖥）/ 关于我们（ℹ️），每行前置 Material 图标 |
| 1.5.2 | 搜索设置 → 蓝牙 | 显示「支持蓝牙 4 传统广播」「支持蓝牙 5 扩展广播」✓ 状态（真机按系统 API 实时检测） |
| 1.5.3 | 搜索设置 → 扫描优先级 | 下拉选择 低/中/高；选择后显示「当前蓝牙扫描模式：低功耗/均衡/低延迟」，且重启扫描生效 |
| 1.5.4 | 搜索设置 → Wi-Fi | 显示「支持/不支持 Wi-Fi NAN」实时状态 |
| 1.5.5 | 关闭蓝牙开关 | 首页扫描面板蓝牙行显示「已关闭」；顶部按钮变为「扫描已关闭」（若 Wi-Fi 也关） |
| 1.5.6 | 显示设置 | 仅「阻止机器休眠」开关；开启后屏幕常亮（息屏时间设置失效） |
| 1.5.7 | 关于我们 | 应用信息、GitHub 链接、演示模式开关；版本号 v1.0.0 显示在页面底部 |

### 1.6 深色模式

| # | 步骤 | 预期 |
|---|------|------|
| 1.6.1 | 系统切换深色模式 | App 界面（背景/卡片/文字）跟随切换，无刺眼亮色块 |
| 1.6.2 | 深色下地图 | 占位地图为深色底；高德地图为标准深色地图样式 |
| 1.6.3 | 页面切换动画 | 前进页面向左滑入，返回页面向右滑出（280ms 过渡） |

### 1.7 页面导航动画

| # | 步骤 | 预期 |
|---|------|------|
| 1.7.1 | 首页 → 详情/设置 | 新页从左滑入 + 淡入 |
| 1.7.2 | 返回首页 | 旧页从右滑回 + 淡出，返回箭头为标准 M3 IconButton（48dp 点击区 + 水波纹） |

---

## 2. 真实 RID 信号测试（有无人机时）

> RID（Remote ID）为 ASTM F3411 标准广播，需要**真正发射 RID 信号的合规无人机**（或 RID 测试信标）靠近手机（蓝牙 2.4GHz，通常 100m 内）。

1. 打开 App，授予全部权限，确认右上角为绿色「扫描中」
2. 让无人机（或测试信标）起飞/开机，靠近手机
3. 预期（10 秒内）：
   - 底部列表出现新设备，呼号为序列号后四位
   - 地图上出现对应彩色标记（按真实经纬度）
   - 点进详情可看到序列号、速度、气压/地理高度、航向等**解包后的真实字段**
4. 无设备时的正确表现：列表空状态、无地图标记——**不会出现虚假数据**

### 常见问题排查

| 现象 | 可能原因 | 处理 |
|------|---------|------|
| 地图空白/「鉴权失败」 | Key 未绑定正确 SHA1/包名 | 按 0.2 检查并重新绑定，重建 APK |
| x86 模拟器启动崩溃 | 高德 SDK 无 x86 原生库 | 换真机或 ARM64 镜像 |
| 扫描不到设备 | 权限未授予 / 蓝牙或 Wi-Fi 关闭 / 附近无 RID 广播 | 检查扫描面板错误提示；开启演示模式验证界面 |
| 扫描按钮显示「扫描异常」 | 蓝牙不可用/未开启或扫描回调失败 | 查看面板错误码，前往搜索设置检查 |
| 详情页导航按钮置灰 | 该设备未广播位置（仅有序列号） | 属正常：位置未知时不可导航 |

---

## 3. 测试结果记录建议

逐条对照「功能测试用例」编号记录 ✅/❌，重点回归：
- 权限拒绝 → 授权 全流程
- 扫描面板开关与外部点击关闭
- 底部面板拖拽动画与 1/3 常驻
- 演示模式开关与标签
- 深色模式全页面
- 导航按钮调起外部地图
